<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="usertableDAO">

    <insert  id="insetToads" keyProperty="map">
        insert into useraddress(pid,address) values (#{u_id},#{u_address})
    </insert>

</mapper>

